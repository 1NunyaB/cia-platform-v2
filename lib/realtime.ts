/**
 * Case workspace uses `CaseWorkspaceRealtime` (evidence_files, entities, entity_aliases, timeline_events,
 * evidence_clusters for this case). Enable those tables in the `supabase_realtime` publication if refreshes lag.
 */

export {};
